import 'package:flutter/material.dart';

class PNCAccountScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Replace below with your integration logic for linking a PNC account
    return Scaffold(
      appBar: AppBar(
        title: Text('Link PNC Account'),
      ),
      body: Center(
        child: Text('PNC Account linking functionality goes here.'),
      ),
    );
  }
}
